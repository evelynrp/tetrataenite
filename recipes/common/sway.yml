---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - adw-gtk3-theme
        - gvfs-mtp
        - qt5ct
        - thunar-volman
  
  - type: files
    files:
      - source: sway
        destination: /
